## 背景

当前控制效果预设使用数十到数百微秒的仿真窗口，控制周期默认也较长。
在交互调参时，用户希望快速执行短窗口仿真，并以更细粒度观察监控、决策和动作的时序关系。

## 修改内容

- 将Web默认仿真时间调整为`5000ns`。
- 将所有控制效果预设的仿真时间调整为`5000ns`。
- 将控制周期最小值调整为`128ns`，默认值也调整为`128ns`。
- 同步更新UI输入约束和配置帮助文案。

## 非目标

- 不改变L3/MC本地监控周期算法。
- 不改变MPAM控制策略、CBusy反馈策略或调度算法。
- 不新增阶段专用仿真模式、数据模型或UI通路。
