# Change: 优化控制模式与算法说明布局

## Why

控制模式区域当前只剩“无控制/有控制”两档，但分段控件仍按三列布局显示，
右侧留出空白；紧邻的算法说明按钮也容易被误认为第三类控制模式。

## What Changes

- 控制模式分段控件固定为两段式布局。
- 将L3 QoS、3-bit QoS、CBusy反馈和地址交织明确标注为“算法说明”入口。
- 在窄屏下算法说明入口换行显示，避免挤压控制模式。

## Impact

- 只改变Web UI布局和可解释性，不改变仿真参数、控制语义或后端配置生成。
