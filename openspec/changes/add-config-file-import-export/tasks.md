## 1. 规格

- [x] 1.1 定义导出文件格式和导入恢复语义。
- [x] 1.2 明确导入导出复用现有表单参数路径。

## 2. 实现

- [x] 2.1 UI增加导出配置、导入配置按钮和隐藏文件输入。
- [x] 2.2 导出当前`collectParameters()`为版本化JSON文件。
- [x] 2.3 导入JSON文件，校验结构并调用`fillForm()`恢复表单。
- [x] 2.4 导入后刷新说明、诊断和PARTID可视选择。

## 3. 验证

- [x] 3.1 静态测试确认UI入口和导入导出函数存在。
- [x] 3.2 JS语法、OpenSpec和相关pytest通过。
