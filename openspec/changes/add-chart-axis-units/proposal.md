## 背景

当前Web控制台的图表只显示曲线标题、图例和数值刻度。由于纵轴刻度没有单位，横轴也只在右下角显示末端时间，
用户在比较P99、带宽、队列、L3占用比例和QoS时需要凭经验判断单位。

## 目标

- 所有时间序列图显示横轴时间单位。
- 所有时间序列图显示纵轴指标单位。
- 延迟分解柱状图显示横轴类别含义和纵轴单位。
- 保持现有数据模型、仿真模型和图例语义不变。

## 非目标

- 不改变监控采样、控制算法或导出字段。
- 不引入第三方图表库。
- 不重做图表布局或交互。
