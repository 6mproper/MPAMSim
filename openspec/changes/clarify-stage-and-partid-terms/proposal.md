## 背景

当前规格中同时出现两类相似写法：

- `P0`作为阶段验收等级，表示机制可信性完成门槛；
- 部分场景或预设文档中用`P0/P1/P2`指代`PARTID 0/1/2`。

这会让用户和后续AI工具混淆“阶段优先级”和“MPAM标识符”。

## 修改目标

- 明确`P0/P1/P2...`只用于阶段、验收或计划优先级。
- 明确MPAM资源标识必须写成`PARTID N`，不得缩写为`PN`。
- 修正文档中已发现的歧义写法。

## 非目标

- 不修改仿真算法。
- 不修改配置字段、数据模型或UI行为。
- 不重新定义P0验收门槛。
